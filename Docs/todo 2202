
11-02-----------
-Old
Perguntar autor:
Modelo específico do processador Intel® Core™ i7-4770 processor at 3.4 GHz
Quantas execuções para o SA?
Instância R103_co_100 possui clientes sem locker. O gurobi nao conseguiu resultados

Gurobi solução inicial para aproveitar a solução anterior. Solução de entrada. Start solution

Gerar uma solução heurística com o ig, depois pegar os dados e jogar no gurobi

Construtivo: Priorizar os obrigatorios (SERA?)

------------------------------------------
Como é melhor aprimorante, ja é suficiente para ignorar a inconsistencia Se for locker locker tentar juntar
analizar somatório de clientes por locker

----------------------------------------------

TODO: Testar varredura das buscas locais, pra ver se fazerm sentido
Lembrar da questão de interseções de movimentos entre buscas


Analisar parametros

Definir tempo limite;
dar a melhor solução
Mandar o gurobi printar a solução apos um determinado tempo
caminho minimo entre os vertices

fazer o caminho minimo e comparar com a matriz de distancia.
tem que bater.

se não bater, ta quebrando a desigualdade triangular

Tirar a verificação dos dois casos na busca local
d[][] double

11,235145
112,354... (*10)
112,000 (floor)
11,2000 (10)
-------------
d[][] int
11,235145
112,354...(*10)
112,00000 (floor)
112 (int)

double(112)/10

Instância R103_co_100 possui clientes sem locker. O gurobi nao conseguiu resultados


retornar bool nas ls para o rvnd (Estudar o impacto)
    -> No inter, o condensador de locker pode conseguir uma condensação, o que é bom, mas o resultado ser o mesmo.
        Tecnicamente com o algoritmo de caminho mínimo isso deve ser mitigado. Testar

Construtivo: Se colocar locker ve ser tem flex dele na rota e condensar
Visitar as citações do trabalho
Trabalhar a escrita

swap 1 1 : a -> b : Se tiver locker de a na rota b, o custo dele nao precisa nem ser considerado pra ir

Instâncias problematicas

Nunca bati a RC107_co_50 -> AGORA SIM

Testar no gurobi imprescindivelmente (instancias que sempre perdemos):
- C104_co_25 -> rodando

- C107_co_50 -> Essa instância tem um locker sem ninguem (P1). A solução ta meio maluca. Uma rota com P1 -> P2 -> P1, apenas. Tirei o P1 da instância pra ver se ela gera uma solução normal. Talvez essa instância quebre o modelo e o resultado da literatura está errado.
    Pra ela, validado o custo do solver no projeto, o problema ta na heurística, que nao acha a solução.

- R205_co_50 rodando

Pra amanhã:
Inconsistencias da atribuição de locker: Considera sempre o primeiro
Passei a executar o GUROBI com print periodico do melhor custo, e encerrei quando ele batia o custo da literatura.
Tw do locker e do deposito não são considerados. Isso impedia bater a RC107_co_50